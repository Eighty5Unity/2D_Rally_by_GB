using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputController 
{
    private readonly PlayerData _model;

    public InputController(PlayerData model)
    {
        _model = model;
    }
}
